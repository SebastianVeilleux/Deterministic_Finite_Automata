// Comentario inicial
x = 10 // Asignación simple
y = 3.14 // Número flotante
z = -2.71E-3 // Flotante en notación científica
a = b + c // Variables con operadores
resultado = (x + y) * z // Paréntesis y operadores
valor = x^2 // Potencia
m = 7 / (3 - 1) // División con paréntesis
// Esto es otro comentario

// Casos complejos
k = -5.67E8 + 2.1E-3
h = .12 * -45
nombre_var = 42
